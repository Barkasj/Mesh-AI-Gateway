{
  "$schema": "https://schema.mesh-ai-gateway.io/config.schema.json",
  "vector_store": {
    "enabled": true,
    "type": "weaviate",
    "config": {
      "scheme": "http",
      "host": "localhost:9000"
    }
  },
  "plugins": [
    {
      "enabled": true,
      "name": "semantic_cache",
      "config": {
        "ttl": 300,
        "threshold": 0.8
      }
    }
  ]
}
